const SectionSubtitle = (props : any) => {
    return (
        <div className="section__body my-1 text-[#74636A]">
              {props.children}
        </div>
    )
  }

export default SectionSubtitle